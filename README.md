# 근대 한옥의 변천 웹전시

진안 강정리 근대 한옥과 강경 구 연수당 건재 약방을 사진과 해설로 살펴보는 정적 웹전시입니다.

## GitHub Pages 배포

`index.html`, `styles.css`, `script.js`를 저장소 루트에 올린 뒤 GitHub Pages의 배포 원본을 `main` 브랜치의 `/ (root)`로 설정합니다.

## 이미지 안내

전시에 사용하는 사진은 `assets/images/`에 포함되어 있어, 압축을 풀어 GitHub 저장소에 올리면 바로 표시됩니다.
